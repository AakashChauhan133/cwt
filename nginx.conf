server {
    listen 80;
    server_name carawintech.com www.carawintech.com;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri.html $uri/ /index.html;
    }
}